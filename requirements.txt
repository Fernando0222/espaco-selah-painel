Django==5.2.17
argon2-cffi==25.1.0
django-axes==8.3.1
requests==2.34.2
django-environ==0.14.0
whitenoise==6.12.0

# Só necessário em produção (Hostinger usa MySQL). Localmente o projeto usa
# SQLite, que já vem com o Python — por isso este pacote não é instalado no
# ambiente de desenvolvimento no Windows.
# Se "mysqlclient" falhar ao instalar no ambiente Linux da Hostinger, troque
# por "pymysql" (ver instruções no guia de deploy).
mysqlclient==2.2.7
