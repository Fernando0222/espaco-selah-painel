Django==5.2.17
argon2-cffi==25.1.0
django-axes==8.3.1
requests==2.34.2
django-environ==0.14.0
whitenoise==6.12.0

# Só necessário quando DB_ENGINE=mysql no .env (uma hospedagem paga com
# MySQL disponível). Deixe comentado enquanto estiver usando SQLite (dev
# local, ou uma hospedagem gratuita sem MySQL, como o plano grátis do
# PythonAnywhere) — em algumas dessas hospedagens instalar esse pacote nem
# funciona, por faltar as bibliotecas de compilação do MySQL no sistema.
# Se "mysqlclient" falhar ao instalar numa hospedagem que TEM MySQL, troque
# por "pymysql" (ver instruções no guia de deploy).
# mysqlclient==2.2.7
